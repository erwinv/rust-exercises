use exer;

fn main() {
    // exer::collatz(6);
    // exer::arrs();
    // exer::tups();
    // exer::refs();
    // exer::dangling_refs();
    // exer::slices();
    // exer::strs();
    // exer::print_fizzbuzz_to(20);
    // exer::rect();
    exer::pick();
}
